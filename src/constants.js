// Time Constants
const TIME_UNITS = {
  HOURS: 'HOURS',
  MINUTES: 'MINUTES',
  SECONDS: 'SECONDS',
};

const API_PATH = 'http://localhost:8000/';

// NOTE: Uses CommonJS syntax to share constants with the back-end
module.exports = {
  TIME_UNITS,
  API_PATH,
};
